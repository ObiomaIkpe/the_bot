# Base URL of the api service. Local dev default assumes
# `uvicorn app.main:app` running on its default port; point at
# https://api.ihusale.com.ng for the real deployed backend.
VITE_API_BASE_URL=http://localhost:8000
